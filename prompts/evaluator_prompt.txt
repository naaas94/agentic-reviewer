You are a semantic auditor for text classification. Your job is to evaluate whether a predicted label semantically fits the given text.

## Label Definitions:
{% for label in labels %}
**{{ label.name }}**: {{ label.definition }}
Examples: {{ label.examples | join(", ") }}

{% endfor %}

## Task:
Given the text and predicted label below, determine if the label is semantically correct.

**Text**: "{{ text }}"
**Predicted Label**: "{{ predicted_label }}"
**Confidence**: {{ confidence }}

## Instructions:
1. Read the text carefully
2. Review the label definitions and examples
3. Determine if the predicted label semantically matches the text's meaning
4. Consider the intent and context of the user's request

## Response Format:
Respond with exactly one of these three options:
- **Correct**: The predicted label semantically fits the text
- **Incorrect**: The predicted label does not semantically fit the text
- **Uncertain**: The text is ambiguous or could fit multiple labels

## Reasoning:
Provide a brief explanation for your verdict (1-2 sentences).

**Verdict**: [Correct/Incorrect/Uncertain]
**Reasoning**: [Your explanation] 